@model ResetPasswordViewModel
@{
    ViewBag.Title = "Şifre Sıfırlama";
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}
@if (TempData["SuccessMessage"]!=null)
{
    <div class="alert alert-success" role="alert">
        @TempData["SuccessMessage"]
    </div>
}
<div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
<form asp-controller="Login" asp-action="ResetPassword" method="post">
    <div class="login__field">
        <label class="form-label" for="Password" asp-for="Password"></label>
        <input asp-for="Password" type="password" class="login__input" />
        <span asp-validation-for="Password" class="text-danger"></span>
    </div>
    <div class="login__field">
        <label class="form-label" for="PasswordConfirm" asp-for="PasswordConfirm"></label>
        <input asp-for="PasswordConfirm" type="password" class="login__input" />
        <span asp-validation-for="PasswordConfirm" class="text-danger"></span>
    </div>
    <button class="button login__submit" type="submit">
        <span class="button__text">Mail Gönder</span>
        <i class="button__icon fas fa-chevron-right"></i>
    </button>
</form>